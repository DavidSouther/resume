# Confirm citations — five missing `refs.bib` keys

Source: `plan.md` Step 0's citation-key checklist, `experiments/worked-analyses.md`.
For each row, open the link, confirm the paper actually supports the claim text,
then fill in `Y` or `N` in the last column. `N` means: find a different paper,
or flag the claim/key for rework in `plan.md` Step 2.

| Key | Worked analysis | Claim in `paper.md` this citation must support | Candidate paper | Authors | arXiv (unverified) | Link | Confirmed? |
|---|---|---|---|---|---|---|---|
| `yao2023react` | §5.5 ReAct / tool-interactive (the load-bearing example) | Interleaving reasoning, tool calls, and observations, where each tool call's *observation* is meant to re-couple the document trajectory to the external referent before the next move — established behavior; the "walks the document manifold toward a goal region" framing is our paper's analogy, not Yao et al's claim. | "ReAct: Synergizing Reasoning and Acting in Language Models" | Yao et al. | 2210.03629 | https://arxiv.org/abs/2210.03629 | Yes, and also HyPE - Hybrid Prompt Expansion on the query side. HyDE expands the target basin, HyPE expands the source rim. |
| `gao2023hyde` | §5.3 HyDE / retrieval expansion | Generating a hypothetical answer/document, embedding it, and retrieving real documents near it improves recall via a denser query neighborhood — established method behavior; the "manufacture an intermediate point in document space" framing is analogy. | "Precise Zero-Shot Dense Retrieval without Relevance Labels" (HyDE) | Gao et al. | 2212.10496 | https://arxiv.org/abs/2212.10496 | Yes. LaTS is similar to the Jeopardy! Search extension of the HyDE->HyPE research. Jeopardy! Search does the tree branches, but what LaTS does with backtracking is like stopping when it appears a trajectory isn't going to work out, and backs off to restart rather than fanning out again. This is a great analogy for our work that isn't in the blog post. |
| `vake2025hype` | §5.3 HyDE / retrieval expansion (HyPE companion citation) | Generating nearby queries around the original ask (rather than a hypothetical answer/document) to expand the search — the query-side/"source rim" counterpart to HyDE's answer-side/"target basin" expansion (see the 2026-07-08 addition to `worked-analyses.md` §3). | Confirmed same paper as `worked-analyses.md` l.99's HyPE reference — use the DOI, not the IEEE Xplore document-ID link. Title/authors still unverified (IEEE Xplore page didn't return fetchable metadata). | Vake et al. (from key name — unconfirmed) | DOI:10.1109/ACCESS.2025.3589499 | https://doi.org/10.1109/ACCESS.2025.3589499 | |
| `zhou2024lats` | §5.6 LATS / tree-search agents | Expanding a search tree of action sequences with backtracking and value estimates, rather than committing to one trajectory — established search procedure; "regions of the document manifold" is analogy. | "Language Agent Tree Search Unifies Reasoning, Acting, and Planning in Language Models" (LATS) | Zhou et al. | 2310.04406 | https://arxiv.org/abs/2310.04406 | Yes |
| `wang2023selfconsistency` | §5.4 Subagent review / multi-sample search | Sampling many candidates from one prompt and selecting/voting over them (self-consistency / pass@k) improves output quality — established for the sampling result; "branches cover distinct document-space regions" is analogy, and the paper's own predicted failure mode is that branches collapse onto shared prompt/context bias. | "Self-Consistency Improves Chain of Thought Reasoning in Language Models" | Wang et al. | 2203.11171 | https://arxiv.org/abs/2203.11171 | Y |
| `chen2024agentless` | §5.4 Subagent review / multi-sample search (second citation, "or similar") | A second source for the *subagent/multi-sample-selection* half of §5.4, distinct from bare temperature-sampling self-consistency — e.g. genuinely distinct start points (different bindings, providers, or roles) with a per-arm rollup for selection, as opposed to N samples from one shared prompt. `worked-analyses.md` ll.33-35 flags this key itself as tentative — the topic and the "Chen" author may not belong to the same paper. | Uncertain — closest topical match found is "Agentless: Demystifying LLM-based Software Engineering Agents," authored by Xia et al., not Chen. May need a different key/paper (e.g. a mixture-of-agents or multi-agent-debate paper) rather than confirming this one. | Xia et al. (if Agentless) | 2407.01489 (if Agentless) | https://arxiv.org/abs/2407.01489 | N - the Agentless approach actively reduces the explored space of the basin. It could be attempting a domain shift? But their work on removing valid ambiguities from SWEBench and calling that progress makes me toss the entire paper. |
| `du2023debate` | §5.4 Subagent review / multi-sample search (candidate replacement for `chen2024agentless`) | Distinct agent personas/instances debate a shared question over several rounds and revise toward consensus — a genuinely-distinct-start-points case (different roles/personas, not just resampling temperature) with a selection/convergence mechanism, as opposed to N samples from one shared prompt. | "Improving Factuality and Reasoning in Language Models through Multiagent Debate" | Du et al. | 2305.14325 | https://arxiv.org/abs/2305.14325 | Y, and also modern practices on Claude-Runs-Codex. This goes beyond "send in a new direction", and actively changes the manifold (from eg Sonnet to GPT), as each model is its own manifold with its own contours. |
| `wang2024moa` | §5.4 Subagent review / multi-sample search (candidate replacement for `chen2024agentless`) | Multiple LLM agents arranged in layers, each refining the prior layer's outputs before a final aggregation — a genuinely-distinct-start-points case via layered roles rather than plain resampling, with the aggregation step doing the selection. | "Mixture-of-Agents Enhances Large Language Model Capabilities" | Wang et al. | 2406.04692 | https://arxiv.org/abs/2406.04692 | Y - follow up research from du2023debate |

## If a row comes back `N`

Note it inline in this file (a short reason next to `N` is enough), then carry
the replacement key/paper into `plan.md` Step 2 before writing `refs.bib` —
Step 2 is explicitly gated on verified metadata, not on this checklist's
tentative IDs.
